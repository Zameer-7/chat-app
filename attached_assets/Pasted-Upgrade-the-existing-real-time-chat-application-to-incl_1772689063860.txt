Upgrade the existing real-time chat application to include a complete user authentication and friend system similar to social chat platforms.

Keep the existing architecture:
Frontend: React + Vite + Tailwind
Backend: Node.js + Express
Database: PostgreSQL
Real-time messaging: WebSockets

Add the following features:

1. User Authentication

Create a login and signup system.

Signup fields:
- email
- password
- nickname (display name)

Password must be securely hashed using bcrypt.

Authentication should use JWT tokens.

Pages required:
- Signup page
- Login page

After login, redirect users to the main dashboard.

2. User Profiles

Each user should have:
- id
- email
- nickname
- created_at

3. Search Users

Create a page where users can search other users by nickname.

Add a search input that queries the backend and returns matching users.

4. Friend Request System

Add the ability to send and receive friend requests.

Friend request table should include:
- id
- sender_id
- receiver_id
- status (pending, accepted, rejected)
- created_at

Functions:
- send friend request
- accept friend request
- reject friend request

5. Friend List

Users should have a page called "Friends".

This page shows:
- accepted friends
- online/offline status

Add buttons:
- Add Friend
- Search User

6. Direct Messaging (Private Chat)

Allow users to chat privately with friends.

Create a private chat channel between two users.

Messages table should support:
- sender_id
- receiver_id
- message
- timestamp

Messages should be delivered in real time using WebSockets.

7. Existing Room Chat

Keep the existing room chat system.

Users should be able to:
- create rooms
- join rooms with room ID
- chat with multiple users

8. Navigation UI

Create a sidebar navigation similar to messaging apps:

Sidebar should include:
- Dashboard
- Rooms
- Friends
- Friend Requests
- Search Users
- Logout

9. Dashboard Layout

Dashboard should show:
- list of friends
- active chats
- option to create or join rooms

10. Security

Ensure:
- JWT authentication for API routes
- password hashing with bcrypt
- user authentication middleware

11. UI Design

Use Tailwind CSS to create a modern messaging interface.

Include:
- chat bubbles
- timestamps
- scrollable chat window
- friend list sidebar

Generate clean, modular code with proper folder structure:

server/
  routes/
  models/
  websocket/
  middleware/

client/
  pages/
  components/
  services/

Explain where the authentication logic and WebSocket logic are implemented.